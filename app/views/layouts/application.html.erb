<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <link href='http://fonts.googleapis.com/css?family=Alef' rel='stylesheet' type='text/css'>
  
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  
  <meta name="keywords" content="<%= meta_keywords %>" /> 
  <meta name="description" content="<%= meta_description %>" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body class="<%= body_classes %>">

<div class="navbar navbar-inverse">
  <div class="navbar-inner">
    <%= link_to "Suit Up & Show Up", root_path, class: "brand" %>
    
    <%= form_tag meetings_path, :method => :get, class: "meetings-search-form navbar-form pull-left form-inline" do %>
      <div class="input-prepend">
        <span class="add-on"><i class="icon-map-marker"></i></span>
        <%= text_field_tag :search, params[:search], placeholder: "#{ @user_location.ip }", class: "input" %>
      </div>
      <%= select_tag :distance, options_for_select([['2 Miles', 2], ['5 Miles', 5], ['10 Miles', 10], ['25 Miles', 25], ['100 Miles', 100]], 5) %>
      <%= submit_tag "Search", :name => nil, class: "btn" %>
    <% end %>
    
    <ul class="nav pull-right">
      <% if current_user %>
        <li><p class="navbar-text">Logged in as <%= current_user.email %>.</p></li>
        <li><%= link_to "Log out", log_out_path %></li>
        <li class="divider-vertical"></li>
        <li>
          <%= link_to new_meeting_path do %>
            <i class="icon-plus icon-white"></i> New Meeting 
          <% end %>
        </li>
      <% else %>
        <%#= link_to "Sign up", sign_up_path %>
        <li><%= link_to "log in", log_in_path %></li>
      <% end %>
    </ul>
  </div>
</div>

<% flash.each do |name, msg| %>
  <div class="container"><div class="row"><div class="span12">
    <div class="alert">
      <button type="button" class="close" data-dismiss="alert">&times;</button>
        <%= content_tag :p, msg, :id => "flash_#{name}" %>
    </div> 
  </div></div></div> 
<% end %>


<%= yield %>


<footer>
    <div class="footer">
       <div class="container">
          <div class="row">
            <hr class="span12" />
            <div class="span12">
              <p>Designed and built with all the love in the world by <%= link_to "@saddacracker", "https://twitter.com/saddacracker", target: "_blank" %></p>
            </div>
          </div>
       </div>
    </div>
</footer>

</body>
</html>




